.env
__pycache__/
*.pyc
*.log
.venv/
venv/
# Modelo grande de pactivo (~537 MB) — se sube manualmente vía SFTP a gestor_oc.
# El de descarte (~6 MB) sí va al repo.
modelo_pactivo.joblib
# Modelo de marcas (~76 MB) — entrenado y persistido en /opt/ia-mp del host
# (ver entrenar_marcas.py). Auto-deploy lo copia al image en el build.
modelo_marcas.joblib
