# Instalar con: pip install -r requirements.txt
# Usar la última versión del SDK de Anthropic (necesita messages.parse / salida estructurada).
anthropic>=0.69.0
pymysql>=1.1.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.6.0
python-multipart>=0.0.9
# Verificación de contraseñas $2y$10$ (bcrypt) de la tabla users de Laravel.
bcrypt>=4.1.0
# Cookies de sesión firmadas para el login del panel.
itsdangerous>=2.2.0
# Clasificador de descarte (entrenado con el histórico etiquetado).
scikit-learn>=1.5.0
joblib>=1.4.0
# Scripts legacy portados de gestor_oc (bin/*.py): leen Excel/CSV y escriben a
# Clásico/Prime con pandas+sqlalchemy. vault_linux_helper usa mysql-connector.
pandas>=2.2.0
sqlalchemy>=2.0.0
openpyxl>=3.1.0
mysql-connector-python>=9.0.0
